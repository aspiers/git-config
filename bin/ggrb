#!/bin/bash

if [ "$1" = '--help' ]; then
    exec git rebase "$@"
fi

if [ $# -eq 0 ]; then
    # default to rebasing against upstream
    if upstream="$( ggup )"; then
        set "$upstream"
    else
        exit 1
    fi
fi

exec git rebase "$@"
