#!/bin/sh

# Diff Tree --stat

[ -t 1 ] && args=--color

if [ -z "$1" ]; then
    upstream="`ggup`" || exit 1  # ggup will display the error
    set -- "$upstream"..HEAD
fi

. $ZDOTDIR/.shared_rc.d/git none
exec git diff-tree --stat $args "$@"
